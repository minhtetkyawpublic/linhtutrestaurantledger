# Bundled font archive

The Padauk font files are retained as licensed repository assets only. The
current application does not load them and no PDF generation feature exists.
See `OFL.txt` for the font license.
